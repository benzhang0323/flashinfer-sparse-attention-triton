[solution]
name = "sparse-attn-v0"      # Solution name
definition = "sparse_attention"          # Track: fused_moe | sparse_attention | gated_delta_net
author = "ben-zhang"              # Team/author name

[build]
language = "triton"               # triton | cuda
entry_point = "kernel"            # Kernel function name
